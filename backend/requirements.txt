fastapi==0.111.0
uvicorn[standard]==0.29.0

# Database
sqlalchemy==2.0.30
alembic==1.13.1
psycopg2-binary==2.9.9

# Auth — bcrypt pinned to 4.0.1 to prevent passlib hash conflict
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-jose[cryptography]==3.3.0
python-multipart==0.0.26

# Admin dashboard
sqladmin==0.16.1

# HTTP client (kept for any direct API calls)
httpx==0.27.0

# LangChain for AI orchestration and prompt templating
langchain-core>=0.3.0
langchain-groq>=0.2.0

# Email delivery (no version pin — resend==2.4.0 does not exist on PyPI)
resend

# Rate limiting
slowapi==0.1.9

# Config & validation
pydantic[email]==2.7.1
pydantic-settings==2.2.1
python-dotenv==1.0.1
