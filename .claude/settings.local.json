{
  "permissions": {
    "allow": [
      "Bash(grep -rn \"datetime.utcnow\" app/)",
      "Bash(grep -rn \"from sqlalchemy import.*text\" tests/)",
      "Bash(grep -rn \"MagicMock\" tests/)",
      "Bash(grep \"^pydantic==\" requirements.txt)",
      "Bash(grep -n \"from app.models.user import UserRole\" app/api/v1/admissions.py)"
    ]
  }
}
